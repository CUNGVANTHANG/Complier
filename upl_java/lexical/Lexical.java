package main.lexical;

import main.model.Parsec;

public class Lexical {

    public String head(Parsec parsec) {
        String c = "\0";

        if (parsec.getPos() < parsec.getN()) {
            String input = parsec.getInput();
        }


        return c;
    }
}
